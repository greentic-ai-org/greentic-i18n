PR-06: CLI contract + integration tests (locks UX + JSON schema)
Goal

Make the CLI a reliable debug tool for the rest of Greentic.

Scope
crates/greentic-i18n

Ensure CLI uses core’s canonicalization + resolution outputs (no re-parsing logic divergence)

JSON output should include:

id, profile, fallback_chain, and sample formatted outputs

Add integration tests via assert_cmd:

normalize

id

resolve --json

complex example parses and prints expected key fields

Acceptance

CLI behavior is locked; future refactors can’t silently change outputs

PR-06 question (CLI JSON schema: doc vs tests)

Do both: publish a lightweight CLI JSON schema document (e.g., `docs/greentic-i18n-cli-json-v1.md`) declaring `schema_version: "v1"` plus the required fields (`id`, `profile`, `fallback_chain`, sample formatted outputs), and keep `assert_cmd` integration tests to verify the emitted contract with each run.

Recommendation: treat the doc as the authoritative spec for downstream consumers, and let the tests enforce it so future changes can evolve via versioned JSON contracts rather than guesswork.
